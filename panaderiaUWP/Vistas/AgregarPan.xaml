<Page
    x:Class="panaderiaUWP.AgregarPan"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:panaderiaUWP.Vistas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Add" Label="AppBarButton" Click="AppBarButton_Click"/>
            <AppBarButton Icon="Cancel" Label="AppBarButton" Click="AppBarButton_Click2"/>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid>
        <StackPanel>
            <TextBox x:Name="ID" PlaceholderText="ID Pan"/>
            <TextBox x:Name="nombrePan" PlaceholderText="Nombre"/>
            <TextBox x:Name="precio" PlaceholderText="Precio"/>
            <TextBox x:Name="cantidad" PlaceholderText="Cantidad"/>
            <Button Content="Subir imagen" Click="Capturar"/>
            <Image x:Name="Imagen1" Width="360" Height="303" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Page>
